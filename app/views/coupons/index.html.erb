<div>
  <% @coupons.each do |coupon| %>
    <div><%= link_to "#{coupon.store} - #{coupon.coupon_code}", coupon_path(coupon) %></div>
  <% end %>
</div>

<div><%= link_to "Create New Coupon", new_coupon_path %></div>
